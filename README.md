# Terraform Provider: GitLab Commits

A custom Terraform provider for working with GitLab Commits API. Combines multiple file changes into a single commit, avoiding multiple push events and CI/CD pipeline triggers.
